import { KiiFileSizePipe } from './kii-file-size.pipe';

describe('KiiFileSizePipe', () => {
  it('create an instance', () => {
    const pipe = new KiiFileSizePipe();
    expect(pipe).toBeTruthy();
  });
});
