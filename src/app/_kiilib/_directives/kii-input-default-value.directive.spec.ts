import { KiiInputDefaultValueDirective } from './kii-input-default-value.directive';

describe('KiiInputDefaultValueDirective', () => {
  it('should create an instance', () => {
    const directive = new KiiInputDefaultValueDirective(null,null);
    expect(directive).toBeTruthy();
  });
});
