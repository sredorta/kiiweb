import { KiiInputDigitOnlyDirective } from './kii-input-digit-only.directive';

describe('KiiInputDigitOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new KiiInputDigitOnlyDirective(null);
    expect(directive).toBeTruthy();
  });
});
