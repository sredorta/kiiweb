import { KiiScrollDirective } from './kii-scroll.directive';

describe('KiiScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new KiiScrollDirective(null,null,null);
    expect(directive).toBeTruthy();
  });
});
