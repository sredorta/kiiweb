import { KiiClickStopPropagationDirective } from './kii-click-stop-propagation.directive';

describe('KiiClickStopPropagationDirective', () => {
  it('should create an instance', () => {
    const directive = new KiiClickStopPropagationDirective();
    expect(directive).toBeTruthy();
  });
});
