import { KiiInputErrorDirective } from './kii-input-error.directive';

describe('KiiInputErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new KiiInputErrorDirective(null,null,null,null,null);
    expect(directive).toBeTruthy();
  });
});
