import { KiiTranslateHttpLoader } from './kii-translate-http-loader';

describe('KiiTranslateHttpLoader', () => {
  it('should create an instance', () => {
    expect(new KiiTranslateHttpLoader(null,null,null)).toBeTruthy();
  });
});
